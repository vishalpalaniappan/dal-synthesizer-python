# dal-synthesizer-python
This tool synthesizes a AST generated by parsing a DAL script into a python program. In addition to synthesizing computable primitives, it also synthesizes developer registered realization of semantics.
